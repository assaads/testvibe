"""testvibe — scenario-driven, agent-driven real-usage testing for any tool.

Foundation package (Task A1). This module is intentionally minimal: it only
exposes the package docstring and an empty ``__all__``. Public surface
(``scenario``, ``invariant`` decorators, etc.) is added by later engine-core
goals, which will extend ``__all__`` — do NOT import not-yet-existing modules
here or the editable install breaks.
"""

__all__: list[str] = []
