# testvibe:generated - DO NOT edit outside keep blocks; `testvibe upgrade` refreshes this file.
# Known-failure / quarantine list for: $contract.
# testvibe:contract: $contract
#
# Guidance: list known-failing scenario ids here so the testvibe gate auto-pins
# them (xfail strict) instead of failing the build. Entries inside the keep
# block survive `testvibe upgrade`; everything else is regenerated on upgrade.
# testvibe:slot:failures
# testvibe:keep failures
# TODO: add known-failure ids (one per line).
# end testvibe:keep
