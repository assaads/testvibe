# testvibe:generated - DO NOT edit outside keep blocks; `testvibe upgrade` refreshes this file.
# Surface S2 - property / invariant scenarios for: $contract.
# testvibe:contract: $contract
#
# Guidance: define @invariant(...) properties that must hold for ALL valid tool
# invocations (round-trip stability, idempotence, monotonicity, ...). Pair with
# hypothesis for generated cases. Code inside the keep block survives
# `testvibe upgrade`; everything else is regenerated on upgrade.
# testvibe:slot:invariants
# testvibe:keep invariants
# TODO: replace with your @invariant(...) property cases.
# end testvibe:keep
