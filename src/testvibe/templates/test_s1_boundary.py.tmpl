# testvibe:generated - DO NOT edit outside keep blocks; `testvibe upgrade` refreshes this file.
# Surface S1 - boundary (edge-case) scenarios for: $contract.
# testvibe:contract: $contract
#
# Guidance: define @scenario(...) cases that probe the tool's input boundaries
# (empty input, max size, off-by-one, unicode, malformed). Import from
# `testvibe` (`@scenario`, `@invariant`). Code inside the keep block survives
# `testvibe upgrade`; everything else is regenerated on upgrade.
# testvibe:slot:scenarios
# testvibe:keep scenarios
# TODO: replace with your @scenario(...) boundary cases.
# end testvibe:keep
